import 'package:eos_mobile/shared/shared_libraries.dart';

class AppConstants {
  AppConstants._();

  /// Padding por defecto para el contenido en un contenedor (ej. InputDecoration).
  static const kDefaultContentPadding = EdgeInsets.symmetric(
    horizontal: 10.2,
    vertical: 13.2,
  );
}
