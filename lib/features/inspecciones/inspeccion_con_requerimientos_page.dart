import 'package:flutter/material.dart';

class InspeccionConRequerimientosPage extends StatelessWidget {
  const InspeccionConRequerimientosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
