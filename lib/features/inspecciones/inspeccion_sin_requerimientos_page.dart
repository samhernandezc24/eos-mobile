import 'package:flutter/material.dart';

class InspeccionSinRequerimientosPage extends StatelessWidget {
  const InspeccionSinRequerimientosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
