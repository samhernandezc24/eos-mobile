import 'package:flutter/material.dart';

class InspeccionDetailsPage extends StatelessWidget {
  const InspeccionDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
