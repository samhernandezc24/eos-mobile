import 'package:eos_mobile/shared/shared_libraries.dart';

class _Predictive extends StatelessWidget {
  const _Predictive({
    Key? key,
    this.optionsMaxHeight = 200.0,
  }) : super(key: key);

  final double optionsMaxHeight;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
