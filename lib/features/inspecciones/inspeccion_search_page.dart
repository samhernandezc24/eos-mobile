import 'package:flutter/material.dart';

class InspeccionSearchPage extends StatelessWidget {
  const InspeccionSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
