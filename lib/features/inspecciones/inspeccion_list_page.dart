import 'package:flutter/material.dart';

class InspeccionListPage extends StatelessWidget {
  const InspeccionListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
